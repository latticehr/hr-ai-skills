# Skills

Each direct child directory is one installable Agent Skill:

```text
skill-name/
└── SKILL.md
```

Optional `references/`, `scripts/`, and `assets/` directories belong inside the
skill that uses them.

Start from [`templates/SKILL.template.md`](../templates/SKILL.template.md).
